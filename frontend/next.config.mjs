/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['media.istockphoto.com'
      , 'lh3.googleusercontent.com', 'avatars.githubusercontent.com', 'cdn.pixabay.com', 'images.unsplash.com'
    ],

  },
  
};

export default nextConfig;
